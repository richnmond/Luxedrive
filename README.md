
  # LUXEDRIVE

  This is a code bundle for LUXEDRIVE. The original project is available at https://luxedrive-vert.vercel.app/

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
